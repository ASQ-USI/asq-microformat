{! This is dangerous, body should be escaped and filter for Javascript. 
Authors of questions are responsible!}
{body|s}

<div class="asq-css-select-code">
  <pre><code>{code}</code></pre>
</div>

<label class="control-label" for="inputText">Selector</label>
{! TODO The id here seems wrong, this should be changed !}
<input type="text" class="" id="inputText" placeholder="Your solution"{?submission}value="{.submission}" disabled{/submission}/>