<ol class='asq-options'>
  {#questionOptions}
    <li class="{classList}" >
      <label class="{formButtonType}">{~n}
        <input type="{formButtonType}" name="{htmlId}" value="{$idx}"
        {?submission} disabled{?submission[$idx]} checked{/submission[$idx]}{/submission}> {text|s}{~n}
      </label>
    </li>{~n}
  {/questionOptions}
</ol>